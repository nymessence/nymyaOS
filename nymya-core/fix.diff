// Syscall entry/exit logging
#define log_syscall_enter(syscall_id, qubit_id) nymya_event_class_syscall_enter(syscall_id, qubit_id)
#define log_syscall_exit(syscall_id, return_code) nymya_exit_syscall_print_funcs(syscall_id, return_code)
--- a/complex_exp_i.c
+++ b/complex_exp_i.c
@@ -8,8 +8,8 @@
  * @theta: The angle in radians, as a Q32.32 fixed-point value.
  * Returns the complex number representing e^(i*theta).
  */
-complex_double complex_exp_i(int64_t theta) {
-    return make_complex(fixed_cos(theta), fixed_sin(theta));
+complex_double complex_exp_i(double theta) {
+    return make_complex(cos(theta), sin(theta));
 }
